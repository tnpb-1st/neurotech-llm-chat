CREATE TABLE V1 (
    REF_DATE TIMESTAMP WITH TIME ZONE NOT NULL,
    TARGET INT NOT NULL CHECK (TARGET IN (0, 1)),
    SEXO CHAR(1),
    IDADE FLOAT,
    VAR4 CHAR(1),
    ESTADO CHAR(2),
    CLASSE CHAR(1),
    CONSTRAINT chk_target CHECK (TARGET IN (0, 1))
);